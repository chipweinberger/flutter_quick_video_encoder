name: flutter_quick_video_encoder
description: Encode raw RGB images & PCM audio to mp4 video or m4a audio using hardware h264 & AAC encoders
version: 1.3.0
homepage: https://github.com/chipweinberger/flutter_quick_video_encoder

environment:
  sdk: ">=2.15.1 <4.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.lib.flutter_quick_video_encoder
        pluginClass: FlutterQuickVideoEncoderPlugin
      ios:
        pluginClass: FlutterQuickVideoEncoderPlugin
      macos:
        pluginClass: FlutterQuickVideoEncoderPlugin

